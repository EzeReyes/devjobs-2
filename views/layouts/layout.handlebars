<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta httpEquiv="X-UA-Compatible" content="IE=edge" />
    <meta name="description" content="Devjobs, un sitio para desarrolladores, registrate y crea tu vacante de empleo o postulate." />
    <meta name="keywords" content="Devjobs" />
    <meta name="author" content="Ezequiel Reyes" />
    <meta property="og:title" content="Devjobs" />
    <link href="https://fonts.googleapis.com/css2?family=Rubik:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="https://unpkg.com/trix@2.0.8/dist/trix.css">
    <link rel="stylesheet" href="/css/app.css">
    <title>{{nombrePagina}}</title>
</head>
<body>
    
    <div class="nombre-sitio contenedor">
        <h1>
            <a href="/">DevJobs</a>
        </h1>

        {{#if barra}}
            <div class="buscador">
                <form action="/buscador" method="POST">
                    <input type="text" name="q"class="buscar">
                    <input type="submit" value="Buscar">
                </form>
            </div>
        {{/if}}

        

        {{#if cerrarSesion}}
            <div class="cerrar-sesion">
                <p>Hola! {{nombre}}</p>
                {{#if imagen}}
                    <img src="{{imagen}}" alt="{{imagen.nombre}}" class="admin-perfil">
                {{/if}}
                <a href="/administracion">Administración</a>
                <a href="/cerrar-sesion">Cerrar sesión</a>
            </div>
        {{/if}}
        {{#if iniciarSesion}}
        <div class="cerrar-sesion">
            <a href="/iniciar-sesion">Iniciar sesión</a>
        </div>
        {{/if}}
    </div>

    <header class="site-header contenedor separador">
        <h2>{{nombrePagina}}</h2>

        {{#if tagline}}
        <p class="tagline">
            {{tagline}}
        </p>
        {{/if}}

        {{#if boton}}
            <a href="/vacantes/nueva" class="btn btn-azul">
                Publicar Nueva Rutina
            </a>
        {{/if}}
    </header>

    <div class="contenedor-principal contenedor">
        {{#if mensajes}}
            <div class="alertas">
                {{#mostrarAlertas mensajes}}
                {{/mostrarAlertas}}
            </div>
        {{/if}}

        {{{body}}}

    </div>

    <script type="text/javascript" src="https://unpkg.com/trix@2.0.8/dist/trix.umd.min.js"></script> 
    <script src="/dist/bundle.js"></script>   
</body>
</html>